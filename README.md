# LLM-Noise-Filtering-System
An intelligent filtering system that removes noisy and irrelevant data from logs using a hybrid approach combining rule-based filtering and Large Language Models (LLMs). The project focuses on building a real-world AI pipeline and evaluating its performance through manual labeling and accuracy measurement.
